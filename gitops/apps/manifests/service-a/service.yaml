apiVersion: v1
kind: Service
metadata:
  name: service-a
  labels:
    app.kubernetes.io/name: service-a
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: service-a
  ports:
    - name: http
      port: 80
      targetPort: http
